export interface Bus{
    busId?: number,
    busTypeId? : number,
    airConditioned? : number,
    licensePlate? : string,
    seatCount? : number,
    basePrice? : number,
    status? : number,
    busName? : string

    locationId? : number,
    locationName? : string
}